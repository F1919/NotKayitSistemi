@model NotKayitSistemi.Models.Entities.OgrenciDers

@{
    ViewData["Title"] = "Ders Ata";
}

<h3>Ders Ata</h3>

<form asp-action="Create" method="post">

    @Html.AntiForgeryToken()

    <div class="form-group">
        <label asp-for="OgrenciTmlId" class="control-label">Öğrenci</label>
        <select asp-for="OgrenciTmlId"
                class="form-control"
                asp-items="ViewBag.OgrenciTmlId">
            <option value="">-- Öğrenci Seç --</option>
        </select>
        <span asp-validation-for="OgrenciTmlId" class="text-danger"></span>
    </div>

    <div class="form-group mt-3">
        <label asp-for="DersId" class="control-label">Ders</label>
        <select asp-for="DersId"
                class="form-control"
                asp-items="ViewBag.DersId">
            <option value="">-- Ders Seç --</option>
        </select>
        <span asp-validation-for="DersId" class="text-danger"></span>
    </div>

    <div class="mt-4">
        <button type="submit" class="btn btn-success">Kaydet</button>
        <a asp-action="Index" class="btn btn-secondary">Geri</a>
    </div>

</form>
